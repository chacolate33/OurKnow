<header>
    <nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #808080;">
      <div class="container">
        <!--ロゴ-->
         <%= link_to "Our Know", root_path, class: "col-2 text-light" %>
        <!--ナビバー-->
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav ml-auto">

            <!--ユーザーログイン時-->
            <% if @current_user %>
              <li style="white-space: nowrap;">
                <%= link_to " My page", user_path(@current_user.id), class:"fas fa-user nav-link text-light text-center" %>
              </li>
              <li style="white-space: nowrap;">
                <%= link_to " Group", groups_path, class:"fas fa-users nav-link text-light text-center" %>
              </li>
              <li style="white-space: nowrap;">
                <%= link_to " Bookmark", myknow_path, class:"fas fa-star nav-link text-light text-center" %>
              </li>
              <li style="white-space: nowrap;">
                <%= link_to " Log out", logout_path, method: :delete, class:"fas fa-sign-out-alt nav-link text-light text-center" %>
              </li>
            <% else %>
              <li style="white-space: nowrap;">
                <%= link_to " Sign up", signup_path, class:"fas fa-user-plus nav-link text-light text-center" %>
              </li>
              <li style="white-space: nowrap;">
                <%= link_to " Log in", login_path, class:"fas fa-sign-in-alt nav-link text-light text-center" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
  </header>
<div class="container my-3">
  <h2><%= @user.name %>'s Follower Users</h2>
  <% if @users.exists? %>
    <%= render 'users/index', users: @users %>
  <% else %>
    <p>ユーザーはいません</p>
  <% end %>
</div>